const info = (name, value) => console.info(name, value)

const log = (name, value) => console.info(name, value)

const error = (name, value) => console.info(name, value)

module.exports = {
  error,
  info,
  log,
}
